.messages-path{data: {message: {id: message.id}}}
  .message-info
    .info-user-name
      = message.user.name
    .info-time
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .message
    - if message.content.present?
      %p.message-content
        = message.content
    = image_tag message.image.url, class: 'Message__image' if message.image.present?